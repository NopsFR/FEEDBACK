# FEEDBACK landing page (optional)

Static, no build step, no tracking. Host anywhere free (GitHub Pages, Cloudflare Pages, Netlify) or just open
`index.html`. The app never depends on it. Don't upload music here — only this folder.

Regenerate images from `brand/` sources: see `design/generators/` and `brand/marketing/`.
